version: '3'
services:
  db:
    image: harikarajavaram/app_db
  web:
    image: harikarajavaram/app_web
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
